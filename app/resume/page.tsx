import type { Metadata } from "next";
import { PageFooter, PageFrame } from "../../components/editorial/PageFrame";
import { selectedWork, socialLinks } from "../../content/site";
import styles from "../phase-two.module.css";
import { createPageMetadata } from "../../lib/seo";
export const metadata: Metadata = createPageMetadata({ title: "Résumé", description: "Professional record for Chuka Dele-Oyeleru, Strategy & Operations.", path: "/resume" });
export default function ResumePage() { return <PageFrame><header className={styles.hero}><p className={styles.heroIndex}>07 / RÉSUMÉ</p><div className={styles.heroCopy}><h1>Professional <em>record.</em></h1><p>Chuka Dele-Oyeleru works in Strategy &amp; Operations, turning ambiguous, cross-functional work into systems people can use and improve.</p></div></header><section className={styles.resumeSheet}><div className={styles.resumeTop}><div><p className={styles.marker}>CHUKA DELE-OYELERU</p><h2>Strategy &amp; Operations</h2></div><a href={socialLinks.linkedin} target="_blank" rel="noreferrer">View LinkedIn ↗</a></div><div className={styles.resumeRecords}>{selectedWork.map((project)=><article key={project.slug}><p className={styles.marker}>{project.context}</p><h3>{project.organisation}</h3><p>{project.description}</p><ul className={styles.signals}>{project.signals.map((signal)=><li key={signal}>{signal}</li>)}</ul><a href={project.href}>{project.linkLabel} →</a></article>)}</div></section><PageFooter /></PageFrame>; }
